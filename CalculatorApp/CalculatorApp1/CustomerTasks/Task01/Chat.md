## Task01 – Первый коммит стажёра

**Игорь**: Стажёр, старт неплохой: репозиторий уже у тебя локально, Visual Studio открылась, мир пока не горит. Давай сделаем так, чтобы `main` выглядел как живой проект, а не как тикток с чисткой ковра.

**Лера**: Мне нужен калькулятор, который хотя бы здоровается, показывает меню и не падает. Никаких космических функций, просто заготовка.

**Игорь**: Код за тебя уже подготовили в `Program_Task01` и `Calculator.cs`. Твоя задача сейчас — не изобрести математику, а:
- создать первую версию калькулятора (перекопировать файлы `.cs` отсюда в `/CalculatorApp`)
- убедиться, что приложение запускается и выводит меню;
- сделать первый осмысленный коммит в `main`;
- посмотреть историю и увидеть свой след.

Подсказки:
- Запусти проект из Visual Studio и проверь, что калькулятор печатает меню и не падает.
- Когда будешь доволен, зафиксируй изменения одним коммитом с нормальным сообщением (например, `feat: add calculator menu`), а потом взгляни на историю ветки.

**Иван**: Первый нормальный коммит — как первый поход без родителей: страшно, но потом будешь смеяться над этим в git log.

### Чек-лист для стажёра

- [ ] Открыл/а решение `CalculatorApp/CalculatorApp.slnx` в Visual Studio.
- [ ] Создал калькулятор, перекопировав файлы `Calculator.cs` и `Program.cs` из этой папки в `CalculatorApp`.
- [ ] Запустил/а приложение и увидел/увидела меню калькулятора без падений.
- [ ] Сделал/а один осмысленный коммит на ветке `main` с понятным сообщением.
- [ ] Открыл/а историю Git и убедился/убедилась, что твой коммит там есть.

